{
  "name": "gedcomloader",
  "version": "0.0.1",
  "description": "read GEDCOM file and load into backend",
  "source": "index.ts",
  "main": "dist/index.js",
  "module": "dist/index.m.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "load": "node -r dotenv/config ./dist/index.js",
    "cli": "ts-node src/cli.ts",
    "test": "jest",
    "lint": "eslint src/ --ext .js,.jsx,.ts,.tsx",
    "clean": "rm -rf dist build",
    "ts-node": "ts-node"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "parse-gedcom": "^2.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  }
}
